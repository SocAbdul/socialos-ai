# Development-only stack. Production databases and brokers must remain on private networks.
name: socialos

services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-socialos}
      POSTGRES_USER: ${POSTGRES_USER:-socialos}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-local-development-only}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-socialos}"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:8-alpine
    command: redis-server --appendonly yes
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  api:
    build:
      context: ./apps/api
      target: development
    env_file:
      - path: .env
        required: false
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-socialos}:${POSTGRES_PASSWORD:-local-development-only}@postgres:5432/${POSTGRES_DB:-socialos}
      REDIS_URL: redis://redis:6379/0
      MEDIA_STORAGE_PROVIDER: ${MEDIA_STORAGE_PROVIDER:-local-public}
      LOCAL_MEDIA_ROOT: /data/public-media
      MEDIA_PUBLIC_BASE_URL: ${MEDIA_PUBLIC_BASE_URL:-https://media.local.socialos.invalid/media}
    ports:
      - "127.0.0.1:${API_PORT:-8000}:8000"
    volumes:
      - ./apps/api:/app
      - ${LOCAL_MEDIA_HOST_PATH:-./.data/public-media}:/data/public-media
    user: "10001:10001"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: sh -c "alembic upgrade head && uvicorn socialos.main:app --host 0.0.0.0 --reload"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://localhost:8000/health/live', timeout=2)"
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 20s

  worker:
    build:
      context: ./apps/api
      target: development
    env_file:
      - path: .env
        required: false
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-socialos}:${POSTGRES_PASSWORD:-local-development-only}@postgres:5432/${POSTGRES_DB:-socialos}
      REDIS_URL: redis://redis:6379/0
      MEDIA_STORAGE_PROVIDER: ${MEDIA_STORAGE_PROVIDER:-local-public}
      LOCAL_MEDIA_ROOT: /data/public-media
      MEDIA_PUBLIC_BASE_URL: ${MEDIA_PUBLIC_BASE_URL:-https://media.local.socialos.invalid/media}
    volumes:
      - ./apps/api:/app
      - ${LOCAL_MEDIA_HOST_PATH:-./.data/public-media}:/data/public-media:ro
    user: "10001:10001"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: celery -A socialos.infrastructure.tasks.celery_app worker --loglevel=INFO
    healthcheck:
      test:
        - CMD-SHELL
        - /opt/venv/bin/celery -A socialos.infrastructure.tasks.celery_app inspect ping --timeout=20 | grep -q pong
      interval: 30s
      timeout: 45s
      retries: 3
      start_period: 30s

  web:
    build:
      context: ./apps/web
      target: development
    environment:
      API_INTERNAL_URL: http://api:8000/api/v1
      NEXT_PUBLIC_API_URL: http://localhost:${API_PORT:-8000}/api/v1
      NEXT_PUBLIC_DEMO_MODE: ${NEXT_PUBLIC_DEMO_MODE:-false}
      NEXT_TELEMETRY_DISABLED: "1"
    env_file:
      - path: .env
        required: false
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./apps/web:/app
      - web_node_modules:/app/node_modules
      - web_next_cache:/app/.next
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test:
        ["CMD", "wget", "--quiet", "--tries=1", "--timeout=30", "--spider", "http://127.0.0.1:3000"]
      interval: 10s
      timeout: 35s
      retries: 5
      start_period: 90s

volumes:
  postgres_data:
  redis_data:
  web_node_modules:
  web_next_cache:
